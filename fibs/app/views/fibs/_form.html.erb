<%= form_for(fib) do |f| %>
  <% if fib.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(fib.errors.count, "error") %> prohibited this fib from being saved:</h2>

      <ul>
      <% fib.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.text_field :name, placeholder: 'Fib Name' %>
  <%= f.text_field :sequence_length, placeholder: '# of Fib values to generate' %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
